services:
  stock_runner:
    build: .
    container_name: stock_runner
    environment:
      - FINNHUB_API_TOKEN=${FINNHUB_API_TOKEN}
    volumes:
      - .:/app
      - /app/_build
      - /app/deps
